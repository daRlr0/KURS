# 📚 Каталог курсов - Android приложение

Полнофункциональное Android приложение для просмотра каталога онлайн курсов с поддержкой поиска, фильтрации и избранного.

## 🏗️ Архитектура

Приложение построено на **MVVM (Model-View-ViewModel)** архитектуре с использованием **Repository pattern**.

### Структура проекта:

```
app/src/main/java/com/example/cors/
├── data/                          # Слой данных
│   ├── local/                     # Локальное хранилище (Room)
│   │   ├── dao/                   # Data Access Objects
│   │   ├── entity/                # Room Entity классы
│   │   └── database/              # Database класс
│   ├── remote/                    # Удалённое хранилище (Retrofit)
│   │   ├── api/                   # API интерфейсы
│   │   └── dto/                   # Data Transfer Objects
│   ├── repository/                # Repository классы
│   └── mapper/                    # Мапперы для конвертации данных
├── domain/                        # Бизнес-логика
│   └── model/                     # Domain модели
├── ui/                            # UI слой
│   ├── adapter/                   # RecyclerView adapters
│   ├── CoursesActivity            # Главный экран
│   ├── CourseDetailActivity       # Экран деталей
│   └── FavoritesActivity          # Экран избранного
├── viewmodel/                     # ViewModels
└── utils/                         # Утилиты
```

## 🛠️ Технологический стек

### Основные технологии:
- **Язык:** Java 8
- **Минимальная SDK:** 26 (Android 8.0)
- **Target SDK:** 34 (Android 14)

### Библиотеки:

#### Архитектура:
- **AndroidX Lifecycle** (LiveData, ViewModel) - управление состоянием
- **Repository Pattern** - абстракция источников данных

#### Локальная БД:
- **Room 2.6.1** - локальное хранилище курсов
  - CourseEntity с полями: id, title, provider, duration, level, imageUrl, description, comment, userRating, isFavorite
  - CourseDao с методами для CRUD операций
  - Поддержка LiveData для реактивного UI

#### Сетевые запросы:
- **Retrofit 2.9.0** - HTTP клиент
- **Gson Converter** - парсинг JSON

#### UI:
- **ViewBinding** - type-safe доступ к View
- **RecyclerView** - эффективные списки
- **Material Design Components** - современный UI
- **Material Chips** - фильтры по категориям
- **Glide 4.16.0** - загрузка и кеширование изображений

## ✨ Функционал

### 1. Главный экран (CoursesActivity)
- 📋 Список всех курсов в RecyclerView
- 🔍 Поиск курсов по названию через SearchView
- 🏷️ Фильтрация по уровню сложности (Material Chips):
  - Все
  - Начальный
  - Средний
  - Продвинутый
- ⭐ Добавление/удаление из избранного
- 📱 Обработка состояний: Loading, Empty, Success, Error

### 2. Экран деталей (CourseDetailActivity)
- 🖼️ Полная информация о курсе
- 📝 Поле для комментария пользователя
- ⭐ Оценка курса (RatingBar от 0 до 5)
- 💾 Сохранение комментария и оценки
- ❤️ Добавление в избранное через FAB

### 3. Экран избранного (FavoritesActivity)
- 📑 Список избранных курсов
- 🗑️ Удаление из избранного
- 📴 Полностью offline режим
- 📭 Empty State с подсказкой

## 🎨 UI/UX особенности

### Material Design 3:
- ✅ Material CardView с elevation и rounded corners
- ✅ Material Chips для фильтров
- ✅ FloatingActionButton для быстрого доступа
- ✅ CollapsingToolbar на экране деталей
- ✅ SearchView в ActionBar
- ✅ Ripple эффекты на кликах

### Адаптивность:
- ✅ NestedScrollView для длинного контента
- ✅ ConstraintLayout для гибкой компоновки
- ✅ Responsive design для разных экранов

### Состояния UI:
- ✅ ProgressBar при загрузке
- ✅ Empty State при отсутствии данных
- ✅ Error State при ошибках
- ✅ Toast уведомления для обратной связи

## 📊 Обработка данных

### Resource<T> Pattern (LCE State):
Приложение использует **Resource<T>** обертку для управления состояниями:

```java
public class Resource<T> {
    enum Status { LOADING, SUCCESS, ERROR }
    private final Status status;
    private final T data;
    private final String message;
}
```

**Преимущества:**
- ✅ Единообразная обработка состояний во всем приложении
- ✅ Type-safe доступ к данным и ошибкам
- ✅ Невозможно получить данные в неправильном состоянии
- ✅ Упрощает код в UI слое (один observer вместо нескольких)

**Использование в ViewModel:**
```java
// Публикация состояний
coursesLiveData.setValue(Resource.loading(null));        // Загрузка
coursesLiveData.setValue(Resource.success(courses));     // Успех
coursesLiveData.setValue(Resource.error("Ошибка", data)); // Ошибка
```

**Обработка в Activity:**
```java
viewModel.getCoursesLiveData().observe(this, resource -> {
    switch (resource.getStatus()) {
        case LOADING: showProgressBar(); break;
        case SUCCESS: showData(resource.getData()); break;
        case ERROR: showError(resource.getMessage()); break;
    }
});
```

### Offline-First подход:
1. **Загрузка:** Данные сначала берутся из локальной БД (быстро)
2. **Обновление:** Параллельно делается запрос к API
3. **Синхронизация:** Новые данные сохраняются в БД
4. **Реактивность:** LiveData автоматически обновляет UI

### Маппинг данных:
```
API (DTO) → Mapper → Room (Entity) → Mapper → UI (Domain Model)
```

### Состояния через Resource<T>:
- **LOADING** - идёт загрузка данных (ProgressBar)
- **SUCCESS** - данные успешно загружены (RecyclerView)
- **ERROR** - произошла ошибка (Toast + старые данные если есть)
- **Empty State** - SUCCESS с пустым списком (заглушка)

## 🔧 Как запустить

### Требования:
- Android Studio Hedgehog или новее
- JDK 8+
- Android SDK 26+

### Шаги:
1. Клонируйте репозиторий
2. Откройте проект в Android Studio
3. Дождитесь синхронизации Gradle
4. Запустите на эмуляторе или реальном устройстве

### Примечание:
- При первом запуске БД автоматически заполнится тестовыми данными
- Для работы с реальным API замените URL в `RetrofitClient.BASE_URL`
- Приложение работает offline благодаря Room кешированию

## 📝 Комментарии в коде

Весь код подробно прокомментирован на русском языке:
- ✅ Каждый класс имеет описание назначения
- ✅ Все публичные методы документированы
- ✅ Сложные алгоритмы имеют пошаговые объяснения
- ✅ Комментарии объясняют "почему", а не "что"

## 🎯 Реализованные паттерны

### MVVM (Model-View-ViewModel):
- **Model:** Repository + Room/Retrofit
- **View:** Activity + ViewBinding + RecyclerView
- **ViewModel:** Управление UI логикой и LiveData

### Repository Pattern:
- Единая точка доступа к данным
- Абстракция источников (БД/API)
- Упрощает тестирование

### Observer Pattern:
- LiveData для реактивного UI
- Автоматическое обновление при изменениях

### Singleton Pattern:
- AppDatabase (один instance на приложение)
- RetrofitClient (переиспользование HTTP клиента)

### ViewHolder Pattern:
- Эффективный RecyclerView
- Кеширование View элементов

### Mapper Pattern:
- Изоляция слоёв приложения
- Конвертация между моделями данных

## 🚀 Возможные улучшения

- [ ] Пагинация для больших списков
- [ ] Swipe-to-refresh для обновления данных
- [ ] SharedPreferences для настроек
- [ ] WorkManager для фоновой синхронизации
- [ ] Navigation Component для навигации
- [ ] Dagger/Hilt для Dependency Injection
- [ ] Coroutines вместо Callbacks
- [ ] DataStore вместо SharedPreferences
- [ ] Jetpack Compose для UI

## 📄 Лицензия

Учебный проект. Свободен для использования в образовательных целях.

## 👨‍💻 Автор

Создано как демонстрация Android разработки с MVVM, Room, Retrofit и Material Design.
